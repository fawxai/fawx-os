# device

Android runtime adapters, event bridges, and action surfaces.

The current executable slice is stock ADB-shell reconnaissance. Rooted Android
and AOSP-level control are explicit adapter targets, not proven runtime
capabilities yet.

This directory should contain only substrate-facing code. The kernel contracts and harness state machines belong elsewhere.
